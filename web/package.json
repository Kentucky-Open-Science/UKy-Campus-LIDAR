{
  "name": "uky-campus-viewer",
  "version": "1.0.0",
  "private": true,
  "description": "Three.js viewer for UKy campus terrain + LiDAR + buildings + procedural city props. Pure static files, no build step.",
  "type": "module",
  "scripts": {
    "start": "http-server . -p 8000 -c-1 -o",
    "serve": "http-server . -p 8000 -c-1",
    "serve:python": "python -m http.server 8000"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
